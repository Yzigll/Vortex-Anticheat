webhookURLs = {
    ban = "", -- Ban
    kick = "", -- Kick
    log = "", -- Log
    server = "", -- Sends server info every 24h (banned count, etc.)
    join = "", -- Sends log when a player joins
    leave = "" -- Sends log when a player leaves
}